#include <iostream>

int main ()
{
    int contador = 0;
    while (contador != 13)
    {
        std::cout << contador << "\n";
        contador++;
        // contador ++ = "contador = contador +1" 
    }
    // != (significa diferente) == (significa igual)
}