#include <iomanip>
#include <iostream>

using namespace std;

int main ()
{
    int quant, numero, aux1=0, aux2=0, cont=1;
    cin >> quant;
    for (quant >1; cont <= quant; cont++)
    {
        cin >> numero;
        if (numero >= 1)
        {
            aux1++
        }
    }
    
    return 0;
}