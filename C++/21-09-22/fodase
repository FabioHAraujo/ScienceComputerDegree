#include <iostream>
#include <iomanip>

using namespace std;

int main ()
{
    int pos=0, tam=20, count, aux;
    int array[tam];
    while (count<20)
    {
        cin >> array[pos];
        pos++;
        count++;
    }
    count = 0;
    pos-=1;

        aux = array[pos];


    // for (pos)
    // aux recebe valor oposto a pos
    // aplica o valor de pos na posição oposta a pos
    // aplica o valor de aux em pos

   /*
    while (count<20)
    {
        cout << array[count] << endl;
        count++;
    }
    */
}